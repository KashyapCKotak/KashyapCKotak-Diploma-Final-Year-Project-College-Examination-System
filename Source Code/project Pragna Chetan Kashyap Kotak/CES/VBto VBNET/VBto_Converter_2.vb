' VBto Converter (www.vbto.net) generate this file.

Module VBto_Converter_2

    Public Sub ShowModeless(ByRef pForm As frmLoginSelect)
        If pForm.IsDisposed Then pForm = New frmLoginSelect()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmFeedback)
        If pForm.IsDisposed Then pForm = New frmFeedback()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmTT)
        If pForm.IsDisposed Then pForm = New frmTT()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmSplash2)
        If pForm.IsDisposed Then pForm = New frmSplash2()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmAbout)
        If pForm.IsDisposed Then pForm = New frmAbout()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmSubjects)
        If pForm.IsDisposed Then pForm = New frmSubjects()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmChangePass)
        If pForm.IsDisposed Then pForm = New frmChangePass()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmBranch)
        If pForm.IsDisposed Then pForm = New frmBranch()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmAddUser)
        If pForm.IsDisposed Then pForm = New frmAddUser()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmAskdb)
        If pForm.IsDisposed Then pForm = New frmAskdb()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmDelBranch)
        If pForm.IsDisposed Then pForm = New frmDelBranch()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmDelSub)
        If pForm.IsDisposed Then pForm = New frmDelSub()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmDeleteUser)
        If pForm.IsDisposed Then pForm = New frmDeleteUser()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmNews)
        If pForm.IsDisposed Then pForm = New frmNews()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmAddQst)
        If pForm.IsDisposed Then pForm = New frmAddQst()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmStartExam)
        If pForm.IsDisposed Then pForm = New frmStartExam()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmStudReg)
        If pForm.IsDisposed Then pForm = New frmStudReg()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmViewRptQst)
        If pForm.IsDisposed Then pForm = New frmViewRptQst()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmStudRslt)
        If pForm.IsDisposed Then pForm = New frmStudRslt()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As MDIForm1)
        If pForm.IsDisposed Then pForm = New MDIForm1()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmSelectExam)
        If pForm.IsDisposed Then pForm = New frmSelectExam()
        pForm.Show()
    End Sub
    Public Sub ShowModeless(ByRef pForm As frmExam)
        If pForm.IsDisposed Then pForm = New frmExam()
        pForm.Show()
    End Sub

End Module	' VBto_Converter_2
